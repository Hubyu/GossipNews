<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.moon.gossipnews.MainActivity">

    <FrameLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>
    <RadioGroup
        android:id="@+id/ra_tabs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <RadioButton
            android:id="@+id/hot"
            style="@style/tab_bottom_style"
            android:layout_height="wrap_content"
            android:checked="true"
            android:drawableTop="@drawable/hot_selector"
            android:textColor="@color/textcolor_selector"
            android:text="热推"/>
        <RadioButton
            android:id="@+id/category"
            style="@style/tab_bottom_style"
            android:layout_height="wrap_content"
            android:drawableTop="@drawable/category_selector"
            android:textColor="@color/textcolor_selector"
            android:text="分类"/>
        <RadioButton
            android:id="@+id/circle"
            style="@style/tab_bottom_style"
            android:layout_height="wrap_content"
            android:drawableTop="@drawable/circle_selector"
            android:textColor="@color/textcolor_selector"
            android:text="八卦圈"/>
        <RadioButton
            android:id="@+id/my"
            style="@style/tab_bottom_style"
            android:layout_height="wrap_content"
            android:drawableTop="@drawable/my_selector"
            android:textColor="@color/textcolor_selector"
            android:text="我的"/>
    </RadioGroup>
</LinearLayout>


